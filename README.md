# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

I have modified the template to add a clock that updates the time every second, effectively making it live.

I have also added a Hex Color Picker package so that the date and time have the ability to dynamically change their color according to the user's preference
